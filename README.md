# ArchitecsEngine

## Requirements & Dependencies
Read [REQUIREMENTS](REQUIREMENTS.md) for more information about the Requirements

## Build & Compile
1. Clone the Repository
```bash
git clone https://github.com/DevVenturex/ArchitecsEngine
cd ArchitecsEngine/
```
2. Build it with `Cargo`
```bash
cargo build
```
